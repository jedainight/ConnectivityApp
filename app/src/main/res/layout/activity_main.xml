<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivity" >

    <Button
        android:id="@+id/webpage"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/bluetooth"
        android:layout_alignRight="@+id/bluetooth"
        android:layout_centerVertical="true"
        android:text="Open Webpage" />

    <Button
        android:id="@+id/bluetooth"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/webpage"
        android:layout_centerHorizontal="true"
        android:text="Share to Bluetooth" />

    <Button
        android:id="@+id/sms"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/nfc"
        android:layout_alignRight="@+id/nfc"
        android:layout_below="@+id/nfc"
        android:text="Send SMS" />

    <Button
        android:id="@+id/nfc"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/webpage"
        android:layout_alignRight="@+id/webpage"
        android:layout_below="@+id/webpage"
        android:text="Check NFC" />

    <Button
        android:id="@+id/email"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/bluetooth"
        android:layout_alignLeft="@+id/bluetooth"
        android:layout_alignRight="@+id/bluetooth"
        android:text="Send E-Mail" />

</RelativeLayout>
